services:
  db:
    image: mysql:latest
    container_name: mysql-iot
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: AQUI VAI A SUA SENHA
      MYSQL_DATABASE: iot_db
    ports:
      - "3308:3306"